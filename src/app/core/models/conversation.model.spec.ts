import { Conversation.Model } from './conversation.model';

describe('Conversation.Model', () => {
  it('should create an instance', () => {
    expect(new Conversation.Model()).toBeTruthy();
  });
});
